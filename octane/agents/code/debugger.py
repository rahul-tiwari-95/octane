"""Debugger — analyzes stderr, produces fixes."""

from __future__ import annotations


class Debugger:
    """Phase 1 stub. Full implementation in Session 4 (Code Agent)."""

    async def debug(self, code: str, error: str) -> str:
        return code  # No fix applied
