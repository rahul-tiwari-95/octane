"""Synthesizer — raw results → structured intelligence with sources."""

from __future__ import annotations


class Synthesizer:
    """Phase 1 stub. Full implementation in Session 3 (Web Agent)."""

    async def synthesize(self, raw_results: list[dict]) -> str:
        """Synthesize raw fetch results into structured intelligence."""
        return f"[Synthesizer stub] {len(raw_results)} result(s) to synthesize"
