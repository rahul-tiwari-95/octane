"""Memory Writer — evaluates what to persist and in which tier."""

from __future__ import annotations


class MemoryWriter:
    """Phase 1 stub. Full implementation in Session 5+."""

    async def write(self, content: str, metadata: dict | None = None) -> bool:
        return False  # Nothing persisted in Phase 1
