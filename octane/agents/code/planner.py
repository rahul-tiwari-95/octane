"""Planner — task description → code specification."""

from __future__ import annotations


class Planner:
    """Phase 1 stub. Full implementation in Session 4 (Code Agent)."""

    async def plan(self, task: str) -> dict:
        return {"language": "python", "approach": task, "status": "stub"}
